// Clear data or reset states before running tests
const { After, Before } = require('@cucumber/cucumber');

Before(() => {
  // Clear users or initialize a database if needed
});

After(() => {
  // Clean up after tests
});
